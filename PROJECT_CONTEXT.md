# 项目上下文

## 项目目标

`LEASE Memory Context` 是 SillyTavern 轻量记忆插件，负责维护记忆表格、按楼层批量填表或手动追溯旧聊天、将选定表格总结到“记忆总结”表，并通过独立 Embedding API 完成分片向量检索。

## 技术栈与运行环境

- SillyTavern 第三方前端扩展
- 原生 JavaScript、jQuery、HTML、CSS
- OpenAI 兼容 Embedding API，可选 Rerank API
- 无本地构建流程或 npm 依赖

## 核心文件

- `index.js`：表格数据、主界面、配置、默认总结注入和向量检索注入。
- `backfill_manager.js`：自动批量填表、手动剧情追溯、分批追溯、单表重构、表格优化和追溯进度管理。
- `summary_manager.js`：仅处理记忆表格总结、总结控制台及按聊天保存的进度指针、源行后处理和向量同步。
- `phone-adapter.js`：识别手机插件请求信号，标记手机消息，并接入现有记忆/向量请求拦截链。
- `vector_manager.js`：向量书籍、统一文本切片、Embedding、检索和隐藏数据库持久化。
- `prompt_manager.js`：合并式剧情方案管理；每个方案同时保存表格结构、史官破限、表格总结提示词和追溯提示词，并支持角色绑定。
- `io_manager.js`：表格和配置导入导出。
- `debug_manager.js`：诊断与日志工具。
- `style.css`：现有界面样式。

## 关键设计决策

- 总结来源固定为记忆表格，不读取聊天历史。
- 表格总结进度指针随当前聊天存储，可在“总结控制台”顶部查看和修正；延迟楼层不计入已总结进度。
- 旧楼层隐藏采用独立的“保留最近 N 层”策略，用户可开关、设置 N 并立即执行；不恢复“总结后隐藏上下文”或智能联动计算。
- 日常聊天不解析或执行模型回复末尾的 `<Memory>` 实时填表指令。
- 日常剧情请求可发送未归档的详细表格：白色行发送，绿色已归档行跳过；该链与默认总结兜底、向量检索和最近 N 层聊天上下文并行工作。
- 批量填表按可配置的间隔楼层和延迟楼层处理已完成聊天区间，并保留静默发起、静默保存选项；手动追溯继续保留区间、单表/全部表、分批、重构、表格优化、进度修正和确认流程。
- 默认合并方案名为“LEASE专属”，内置 LEASE 八张表以及用户提供的总结、追溯提示词；自定义方案切换时表结构与提示词同步切换。
- 总结正文按 `vectorSeparator` 切片，默认 `===`；总结行标题和备注不进入向量。
- 总结后的源行可配置为 `keep`、`hide` 或 `delete`。
- 开启 `autoVectorizeSummary` 后，总结保存会同步并向量化；只有当前聊天的向量书存在且全部切片状态均成功时，才抑制默认总结并由语义检索接管。
- 自动向量同步成功后，“记忆总结”表全部行自动归档隐藏；同步或向量化失败时保持白色，不误归档，便于用户重试。
- 未开启自动向量化、API 配置缺失、无向量书或向量化未全部成功时，继续使用“记忆总结（默认发送）”兜底。
- 静默总结关闭时保留可编辑预览和本次源行处理选择；静默总结开启时采用全局 `keep`、`hide` 或 `delete` 设置。
- 表格 JSON/TXT 和向量书均支持导出导入；向量跨聊天恢复后需在书架绑定到当前聊天，并建议保持相同 Embedding 模型。
- 用户可见的世界书总结同步已移除；`Memory_Vector_Database` 仅作为隐藏向量持久化存储。
- 保留原扩展 ID `st_memory_table` 与存储键，兼容已有记忆表格数据；显示名称和维护者已改为 LEASE。
- 公开版本保留原项目 gaigai315 / Gaigai Team 的署名，并通过 `LICENSE`、`ATTRIBUTION.md` 和 README 标明 MIT 授权来源与非官方衍生关系。
- GitHub 写操作只能面向 LEASE 自己的 `LEASE-2473/ST-Memory-Context`；原作者 `gaigai315/ST-Memory-Context` 只能只读参考，禁止向其推送、创建 PR/Issue、评论或产生通知。执行发布前必须阅读 `GITHUB_PUBLISHING_SAFETY.md`。

## 验证方法

- 静态语法：`node --check index.js` 及逐个检查所有 `.js`。
- JSON：解析 `manifest.json`。
- 残留检查：搜索已移除模块的活动加载入口和用户可见 UI。
- 实机验证需要在 SillyTavern 中完成总结、源行处理、向量化和检索注入。

## 当前状态与已知风险

- v3.3.3 已将表格总结控制台和表格选择弹窗恢复为上游布局，并把自动批量填表、自动表格总结重新接回上游带临时顺延的确认弹窗；`backfill_manager.js` 与固定上游快照逐字一致，无需重复修改。自动向量同步成功后会归档隐藏“记忆总结”表，失败时不归档；尚需 SillyTavern 实机回归。
- v3.3.2 已将表格总结预览窗恢复为上游 `#gai-summary-pop` 与 `.g-ov / .g-w / .g-bd` 布局，重新接入现有手机端限高和内部滚动规则；非静默保存后继续使用上游三按钮原行处理框（删除、隐藏、保留），其中删除只作用于本次参与总结的源行。静默模式继续按全局设置直接处理，不创建预览窗或原行处理框；尚需 SillyTavern 手机实机回归。
- v3.3.1 已恢复未归档详细表格的请求注入，并将“💉 注入记忆表格”配置卡、信息图标和变量说明恢复为上游布局；恢复默认提示词入口，修复 TXT 归档状态往返，并清理实时填表回档死分支、幽灵字段和旧表结构 UI 事件。
- 已将原作者 `main` 的固定提交 `28241c9` 下载到 LEASE 魔改资料夹并完成逐项对照；按用户决定不恢复历史实时填表标签发送前清洗、不增加导出全部组合方案和高级设置 UI。
- 已物理删除旧实时消息解析入口和实时回档链；批量填表复用现有追溯管理器及 `<Memory>` 解析执行器，不恢复逐条实时填表。
- 旧内置 gaigai/yuzuki 方案会迁移为“LEASE专属”；用户自行创建的方案保留。
- 当前源码目录已经初始化为 Git 仓库，本地 `main` 跟踪 `origin/main`，其中 `origin` 指向 `https://github.com/LEASE-2473/ST-Memory-Context.git`。当前 GitHub 仓库仍带 Fork 来源标记，但所有写操作必须严格限制在 LEASE 自己的仓库；不得向原作者仓库提交或发起跨仓库 PR。
