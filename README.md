# LEASE Memory Context

面向 SillyTavern 的轻量记忆表格、表格总结与独立向量检索插件。

> 本项目基于 [gaigai315/ST-Memory-Context](https://github.com/gaigai315/ST-Memory-Context) 修改，保留原项目贡献者的署名，并按照原项目 README 声明的 MIT License 发布。本仓库是由 LEASE 维护的非官方衍生版本，与原作者不存在官方隶属或背书关系。详细归属与改动范围见 [ATTRIBUTION.md](ATTRIBUTION.md)。

> GitHub 发布与维护必须遵守 [GITHUB_PUBLISHING_SAFETY.md](GITHUB_PUBLISHING_SAFETY.md)：原作者仓库只用于只读参考和署名，绝不向其推送、提交 PR、Issue、评论或其他会产生影响与通知的内容。

## 保留功能

- 记忆表格的查看、手动编辑、导入导出和自定义表格结构。
- 完整的手动“追溯”工具：历史区间追溯、全部/单表追溯、分批执行、重构模式、表格优化、进度修正和结果确认。
- 可配置的自动批量填表：间隔楼层、延迟楼层、静默发起和静默保存均可独立设置。
- 可配置的未归档详细表格发送：白色行随剧情请求发送，绿色已归档行自动跳过，避免两次批量填表或总结之间出现结构化记忆断档。
- 可配置的旧楼层自动隐藏：用户指定保留最近 N 层，插件自动将更早楼层设为隐藏；不启用“总结后智能联动隐藏”。
- 手机填表插件适配：识别手机请求信号、标记手机消息、跳过手机内部 API，并遵守手机侧的向量权限。
- 合并式剧情方案：每个方案同时保存表格结构、史官破限、表格总结提示词和剧情追溯提示词，并可绑定当前角色。
- 选择一个或多个记忆表格生成总结。
- 自动总结保留间隔楼层、延迟楼层、静默发起、静默保存和源行处理配置；总结进度指针按聊天保存并可手动修正。
- 在总结确认页重新生成结果，再决定是否保存。
- 总结成功后保留、隐藏或删除本次参与总结的源行。
- 将总结写入“记忆总结”表。
- 按配置的分隔符切片，默认使用 `===`。
- 每个切片独立生成 Embedding，并在后续聊天中按相似度检索注入。
- 只有当前聊天的全部总结切片确实向量化成功后，向量检索才接管默认总结；配置不完整、未生成向量书或部分失败时继续使用默认总结兜底。
- 表格 JSON/TXT 与向量书均可独立导出、导入；新聊天可导入历史表格和向量备份后继续使用。

## 已移除功能

- AI 回复末尾的实时填表提示词与 `<Memory>` 自动执行流程。
- 聊天历史总结、分批聊天总结和大总结。
- 总结优化/润色。
- 将总结同步到普通世界书的功能与界面。
- 上游仓库自动更新。

## 总结与向量化流程

1. 如需整理旧聊天，可用“追溯”处理指定历史区间；日常剧情也可开启自动批量填表，按“间隔楼层＋延迟楼层”处理已完成区间。
2. 在“总结”中选择要处理的记忆表格。
3. 选择总结成功后的源行处理方式：保留、隐藏或删除。
4. 总结模型输出被原样写入“记忆总结”表。
5. 向量模块只读取总结正文，不读取“剧情总结 N”等行标题或备注。
6. 正文按向量设置中的分隔符切片；默认 `===`，空片段会被过滤。
7. 每个切片独立向量化，并绑定到当前聊天的《剧情总结归档》。
8. 插件验证当前聊天所有片段均已生成向量后，才停止默认总结发送；后续发送消息时检索相关片段并注入最终请求正文。尚未归档的白色详细表格不受向量接管影响，仍会继续发送。

## 向量数据存储说明

向量数据库仍通过 SillyTavern 的隐藏存储书 `Memory_Vector_Database` 持久化。这只是向量模块的内部存储层，不会作为记忆总结世界书绑定或注入，也不属于已经移除的“世界书总结同步”功能。保留该方式是为了避免浏览器 `localStorage` 容量不足导致向量丢失。

## 安装

在 SillyTavern 的“安装扩展”中使用以下仓库地址：

```text
https://github.com/LEASE-2473/ST-Memory-Context
```

也可以下载仓库 ZIP，将其解压到 SillyTavern 的第三方扩展目录，确保安装目录名与 SillyTavern 实际加载路径一致，然后刷新页面。

## 版本

- 插件版本：3.3.2
- 本地魔改维护者：LEASE
- 原项目：gaigai315 / Gaigai Team 的 ST-Memory-Context
- 许可证：MIT，详见 `LICENSE`
